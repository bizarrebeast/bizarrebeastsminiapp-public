import RitualsClientV2 from './RitualsClientV2';

export const metadata = {
  title: 'Daily Rituals V2 | BizarreBeasts',
  description: 'Complete daily BIZARRE rituals - V2 Layout Test',
  viewport: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no',
};

export default function RitualsV2Page() {
  return <RitualsClientV2 />;
}
