import GalleryClient from './GalleryClient';

export const metadata = {
  title: 'Canvas Paintings Gallery | Dylan - The BizarreBeast',
  description: '50+ original canvas paintings by Dylan, the artist behind BizarreBeasts. Explore the darkly humorous world of outsider art.',
};

export default function GalleryPage() {
  return <GalleryClient />;
}
