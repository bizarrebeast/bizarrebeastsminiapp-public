import AboutClient from './AboutClient';

export const metadata = {
  title: 'About BizarreBeasts | Meet the Creator',
  description: 'Learn about the artist and innovator behind BizarreBeasts - a web3 creative universe built on community, coins, and culture.',
};

export default function AboutPage() {
  return <AboutClient />;
}
